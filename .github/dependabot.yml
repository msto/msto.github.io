version: 2
updates:
  # Keep the SHA-pinned GitHub Actions current (Dependabot updates both the
  # hash and the trailing "# vX.Y.Z" comment).
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    commit-message:
      prefix: "ci"
